framework:
    mailer:
        dsn: '%env(MAILER_DSN)%'
        message_bus: messenger.bus.default
        envelope:
            sender: '%env(MAILER_FROM)%'
        headers:
            From: '%env(MAILER_FROM)%'
